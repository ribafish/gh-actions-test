name: Matrix with use

on:
  workflow_dispatch
  push

jobs:

  timeout-runner:
    uses: ribafish/gh-actions-test/.github/workflows/reusable_workflow.yml@00997d21672669b254168eedc4d3ca4b297777b0
    with:
      arg: thing
      timeout-minutes: 10

  # matrix-runner:
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       args: [1,2,3,4]
  #   steps:
  #     - name: "Matrixed step"
  #       uses: ribafish/gh-actions-test/.github/workflows/reusable_workflow.yml@f25d49dca916279bbe809b677983e703e0877d12
  #       with:
  #         arg: ${{ matrix.args }}
