name: Workflows on push with path
on: 
  push:
    branches:
      - main
    paths:
      - 'testPath/**'
jobs:
  gradle-testPath:
    name: Test path conditionals
    uses: ribafish/action-gradle-run@v1
    with:
      gradle_task: "tasks --all"
      gradle_workDir: 'testPath'
      
