name: Workflows on push with path
on: 
  push:
    branches:
      - main
    paths:
      - 'testPath/**'
jobs:
  gradle-testPath:
    name: Test path conditionals
    uses: ./.github/actions/action_gradle_run.yml
    with:
      gradle_task: "tasks --all"
      workDir: 'testPath'
      
